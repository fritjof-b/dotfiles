


# Close currently focused node
super + shift + q
	bspc node -{c,k}

# Window navigation vim kbs
super + {h,j,k,l}
	bspc node -f {west,south,north,east}


# Dynamic gaps.
mod3 + shift + bracket{left,right}
	 bspc config -d focused window_gap "$(($(bspc config -d focused window_gap) {-,+} 5 ))"


