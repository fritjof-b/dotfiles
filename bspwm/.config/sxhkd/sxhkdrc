# Keybinds for BSPWM

super + Return
	mate-terminal

super + shift + Return
	xterm

ctrl + alt + Delete
	mate-terminal

super + shift + w
	chromium

super + d
	dmenu_run


# quit bspwm normally
super + alt + Escape
	bspc quit
