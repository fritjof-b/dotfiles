# Keybinds for BSPWM


# Basic commands for running applications and window switching

# Run xterm
super + Return
	xterm

# Run firefox
super + shift + w
	firefox-esr

# Run application
super + space
	rofi -show run

# Classic window switcher
alt + tab
	rofi -show window


# quit bspwm normally
super + alt + Escape
	bspc quit
